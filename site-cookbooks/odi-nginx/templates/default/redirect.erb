server {
  listen <%= @port %>;
  server_name <%= @that %>;
  access_log /var/log/nginx/<%= @that %><%= @ssl_tag %>.log;
  error_log /var/log/nginx/<%= @that %><%= @ssl_tag %>.err;
  rewrite  ^/(.*)$ <%= @protocol %>://<%= @this %>/$1 permanent;
}